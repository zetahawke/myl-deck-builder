<%= form_with(model: rarity) do |form| %>
  <% if rarity.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rarity.errors.count, "error") %> prohibited this rarity from being saved:</h2>

      <ul>
        <% rarity.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
